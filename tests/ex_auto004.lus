/* From Heptagon manual */

node updown() = y:int
with var shared x:int in
  y = x;
  automaton
    state Up
      do x = last x + 1
      unless last x >= 10 then Down
    state Down
      do x = last x - 1
      unless last x <= 0 then Up
  end
